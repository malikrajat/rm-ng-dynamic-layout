# Advanced Configuration

Deep dive into configuration options.

- `responsive`: breakpoint-based overrides for columns, gap, areas
- `templateAreas`: multiline strings, desktop/mobile swaps
- Alignment and ordering: per-item controls via inputs/classes
- Data-driven layouts: spans and areas from state